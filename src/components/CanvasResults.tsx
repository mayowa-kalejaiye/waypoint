"use client";

// Keep the public import path stable while the redesigned page lives in CanvasResultPage.jsx.
const CanvasResultPage = require("./CanvasResultPage.jsx").default;

export default CanvasResultPage;
